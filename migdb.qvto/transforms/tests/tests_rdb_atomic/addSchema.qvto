import builder_rdb_link;
modeltype RDB uses "http://www.collectionspro.eu/jam/mm/rdb";
transformation addschema(out inoutModel : RDB);


main() {
	log("TEST_RDB");
	log("op: ADDSCHEMA");	
	
	var model : RDB::ModelRoot := _rdbStructure(OrderedSet{});
}