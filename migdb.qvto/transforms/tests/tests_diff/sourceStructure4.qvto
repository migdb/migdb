import builder_app;
import queries_app;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation sourceStructure4(out inoutModel : APP);

main(){
	var int : PrimitiveClass := _primitiveClass(
												getAppDefaultIdTypeName(), 
												app::PrimitiveType::int);
	var char : PrimitiveClass := _primitiveClass(
												"Char", 
												app::PrimitiveType::char);	
	var personProps : OrderedSet(MultipliableProperty) := OrderedSet{
																_primitiveProperty("age", int),
																_primitiveProperty("name", char)};
	var clasPerson : StandardClass := _class("Person", _idProperty("idParent", int), personProps);
	_appStructure(OrderedSet{int, char, clasPerson});
}