import builder_app;
import queries_app;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation extractClass_ops(out inoutModel : APP);

main(){
	log("op: MOVEPROPERTY");

	_appOperations(OrderedSet{_moveProperty("First", "address", "Second", ToleranceType::tolerant)});	
}