import builder_app;
import queries_app;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation setOpposite1(out inoutModel : APP);

main(){
	var setOpposite : APP::ops::SetOpposite :=_setOpposite("Tab", "tabOp", "Op", "opOp", true); 
	_appOperations(OrderedSet{setOpposite});	
}