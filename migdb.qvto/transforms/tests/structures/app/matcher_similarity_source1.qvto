import builder_app;
import queries_app;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation matcher_similarity_source1(out inoutModel : APP);

main(){
	var int : PrimitiveClass := _primitiveClass(
											getAppDefaultIdTypeName(), 
											app::PrimitiveType::int);
	var string : PrimitiveClass := _primitiveClass(
											"String",
											app::PrimitiveType::char);
	var manProps : OrderedSet(MultipliableProperty) := OrderedSet{
													_primitiveProperty("age", int, 1, 1),
													_primitiveProperty("firstName", string, 1, 1),
													_primitiveProperty("secondName", string, 1, 1),
													_primitiveProperty("car", string, 1, 1)};
	var man : StandardClass := _class(
									"Man", 
									_idProperty("idMan", int),
									manProps); 								
	_appStructure(OrderedSet{int, string, man});
}