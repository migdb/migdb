import builder_app;
import queries_app;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation structure_embedded_hierarchy_TPC(out inoutModel : APP);

main(){
	var int : PrimitiveClass := _primitiveClass(getAppDefaultIdTypeName(), app::PrimitiveType::int);
	var tab : EmbeddedClass := _embeddedClass("Tab", OrderedSet{
		_embeddedProperty("emProperty", int)}, _embeddedIdProperty("id", int));
	var emptyProps : OrderedSet(MultipliableProperty) := OrderedSet{};
	var root : StandardClass := _class("Root", _idProperty("idRoot", int), emptyProps, 
		false, InheritanceType::tablePerClass);
	var child : StandardClass := _class("Child", root, OrderedSet{});
	
	_appStructure(OrderedSet{int, tab, root, child});
}