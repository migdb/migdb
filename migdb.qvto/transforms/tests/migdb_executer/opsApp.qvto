import builder_app;

modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";
transformation opsApp(out outAstr : APP);

main(){
	_appOperations(OrderedSet{ _addStandardClass("cls", false, InheritanceType::joined)});
}