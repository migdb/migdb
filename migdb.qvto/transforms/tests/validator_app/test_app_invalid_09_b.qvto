import builder_app_link;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";
transformation secondAPP(out inoutModel : APP);

main() {
	log("TEST_APP_INVALID_09_B - invalid : nonInteger ID ");	
	var integer : APP::PrimitiveClass := _primitiveClass("Integer", PrimitiveType::int);
	var string : APP::PrimitiveClass := _primitiveClass("String", PrimitiveType::char);
	
	var person : APP::StandardClass := _class("Person", OrderedSet{ _property("name", string)}, _idProperty(integer));
	//TODO check if this test hack is valid
	var idProperty : Property := _property("id", string, 1, 1);
	var thing : APP::StandardClass := _class("Thing", OrderedSet{ _property("name", string), idProperty}, _idProperty(integer));	
	thing.idProperty := idProperty;
	
	var model : APP::ModelRoot := object ModelRoot {
		modelGenerations += object ModelGeneration {
			isMissing := false;
			entities += Set{integer,string,person, thing
			};
		};
	 };
	 
}