import builder_app_link;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";
transformation secondAPP(out inoutModel : APP);

main() {
	log("TEST_APP_INVALID_04_b - invalid : oppositeProperty - wrong type");	
	var integer : APP::PrimitiveClass := _primitiveClass("Integer", PrimitiveType::int);
	var string : APP::PrimitiveClass := _primitiveClass("String", PrimitiveType::char);
	var money : APP::StandardClass := _class("money", OrderedSet{ _property("name", string)}, 
	_idProperty(integer));
	
	var country : APP::StandardClass := _class("Country", OrderedSet{ _property("name", string)}, _idProperty(integer));
	
	var propCountry : APP::Property = _property("debt", integer);
	var propMoney : APP::Property := _property("owningCountry", country);
	propMoney.oppositeProperty := propCountry;
	
	country.properties += propCountry;
	money.properties += propMoney;

	
	var model : APP::ModelRoot := _appStructure(Set{integer,string,country, money});
}