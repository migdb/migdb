import builder_app_link;
modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";
transformation secondAPP(out inoutModel : APP);

main() {
/**	
*	condition "ambiguous property in class" is also violated 
**/
	log("TEST_APP_INVALID_07 - invalid : Non root ID");	
	var integer : APP::PrimitiveClass := _primitiveClass("Integer", PrimitiveType::int);
	var string : APP::PrimitiveClass := _primitiveClass("String", PrimitiveType::char);
	
	var person : APP::StandardClass := _class("Person", OrderedSet{ _property("name", string) }, _idProperty(integer));
	
	var idProp := _idProperty(integer);
	var teacher : APP::StandardClass := _class("Teacher", person, OrderedSet{ _property("sallary", string), idProp});
	teacher.idProperty := idProp;
	
	var model : APP::ModelRoot := _appStructure(Set{integer,string,person, teacher});
}