modeltype APP uses "http://www.collectionspro.eu/jam/mm/app";

transformation build_model(inout inoutModel : APP);


main() {
	// IDDDDD
	// VYKONA SE
	var classParent : APP::operations::AddClass := object APP::operations::AddClass {
														name := "Parent";
														parent := null;
														isAbstract := false;
														isEmbedded := true;
														tableName := null;
														inheritanceType := APP::InheritanceType::__not_defined;
												  };	

	// VYKONA SE
	var classChild : APP::operations::AddClass := object APP::operations::AddClass {
														name := "Child";
														parent := null;
														isAbstract := false;
														isEmbedded := true;
														tableName := null;
														inheritanceType := APP::InheritanceType::__not_defined;
												  };	
	// SETPARENT									
	// VYKONA SE
	var setparent : APP::operations::SetParent := object APP::operations::SetParent {
														name := "Child";
														parentName := "Parent";
												  };	
	var addproperty : APP::operations::AddProperty := object APP::operations::AddProperty {
														owningClassName := "Child";
														name := "Pokus";
														type := "Parent";
														defaultValue := null;
														lowerBound := 1;
														upperBound := 1;
														isOrdered := false;
														isUnique := false;
														columnName := null;
														columnNamePattern := null;
														sequenceName := null;
												  };		
	var removeproperty : APP::operations::RemoveProperty := object APP::operations::RemoveProperty {
														owningClassName := "Child";
														name := "Pokus";
														
												  };													  									  												  												  									  	
	
	var model : APP::ModelRoot := object ModelRoot {
		modelGenerations += object ModelGeneration {
			isMissing := false;
		};
	 };
	model.operations += classParent;
	model.operations += classChild;
	model.operations += setparent;
	model.operations += addproperty;
	model.operations += removeproperty;
	// model.operations += class1;
	// model.operations += class2;
	// model.operations += class3;
	// model.operations += class4;
	// model.operations += class5;
	// model.operations += class6;
	// model.operations += class7;
	// model.operations += class8;
}